{
    "scripts": {
        "start": "parcel src/index.html --global app",
        "build": "parcel build src/app.js --experimental-scope-hoisting --out-dir build --global app"
    },
    "dependencies": {
        "lit-html": "^1.3.0",
        "@fortawesome/fontawesome-free": "^5.15.1"
    },
    "devDependencies": {
        "parcel-bundler": "^1.12.4",
        "sass": "^1.29.0"
    }
}
